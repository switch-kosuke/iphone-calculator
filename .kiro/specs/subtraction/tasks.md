# Implementation Plan

- [x] 1. (P) 引き算演算ロジックを演算エンジンに実装する
  - 演算エンジンの `calculate()` メソッドに `'-'` 演算子のブランチを追加する
  - `parseFloat((a - b).toPrecision(12))` を用いて浮動小数点誤差を補正した結果を返す
  - 引数が有限数でない場合のガード節は既存実装で対応済みのため変更不要
  - 指数表記（`formatDisplay`）も既存実装で対応済みのため変更不要
  - タスク 2.1 と並行実行可能（異なるファイルを変更する）
  - _Requirements: 2.1, 2.2, 2.3, 2.4_

- [x] 2. 引き算UIとコントローラーを統合する

- [x] 2.1 (P) 引き算ボタンをHTMLレイアウトに追加する
  - 行3（4・5・6 の右）の `<div></div>` プレースホルダーを引き算ボタンに置き換える
  - ボタンに `id="btn-subtract"` および `data-action="subtract"` を付与する
  - スタイルは既存の `+` ボタンと同一（丸型・オレンジ背景・白文字・ホバーエフェクト）を使用する
  - `aria-label="引く"` を付与してアクセシビリティを確保する
  - グリッドレイアウト（`grid-cols-4`）への影響なし（要素数変化なし）
  - タスク 1 と並行実行可能（異なるファイルを変更する）
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2.2 引き算ボタンのクリックイベントとキーボード対応を追加する
  - `btn-subtract` に `click` イベントリスナーを追加し、引き算演算子で操作を処理する
  - リスナーは `+` ボタンのリスナー直後に配置してコードの局所性を維持する
  - `keydown` ハンドラに `-` キーの分岐を追加し、ブラウザデフォルト動作を抑止する
  - 各イベント後に画面を再描画して表示を更新する
  - タスク 2.1 完了後に実行すること（`btn-subtract` の DOM要素が必要）
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 5.1, 5.2_

- [x] 2.3 引き算選択中の演算子ハイライト表示を実装する
  - `render()` 内のハイライト処理を `−` ボタンにも適用する
  - `−` ボタンは `−` 演算子が選択中かつ第2オペランド待ち状態のときに白背景・オレンジ文字で表示する
  - それ以外の状態では `−` ボタンをオレンジ背景・白文字（デフォルト）で表示する
  - 既存の `+` ボタンのハイライトロジックは変更せずに維持する
  - タスク 2.2 と同一ファイルを変更するため、2.2 完了後に実行すること
  - _Requirements: 4.1, 4.2, 4.3_

- [x] 3. 引き算機能のテストを追加する

- [x] 3.1 (P) 演算エンジンの引き算テストを追加する
  - エンジンテストに整数の引き算（例: 8 − 3 = 5）のテストケースを追加する
  - 浮動小数点補正の確認（例: 0.3 − 0.1 = 0.2）のテストケースを追加する
  - 負の結果になる引き算（例: 3 − 8 = −5）のテストケースを追加する
  - 非有限数を引数に渡した場合にエラー値が返ることを確認するテストケースを追加する
  - タスク 3.2 と並行実行可能（異なるテストファイルを変更する）
  - タスク 1 完了後に実行すること
  - _Requirements: 6.1_

- [x] 3.2 (P) 状態フローの引き算テストを追加する
  - 基本的な引き算フロー（数値入力 → 引き算 → 数値入力 → イコール）のテストケースを追加する
  - イコール連打による繰り返し計算（例: 8 − 3 = → 5、再度 = → 2）のテストケースを追加する
  - `+` 選択後に `−` に切り替えた場合の演算子変更テストケースを追加する
  - `−` 選択中に AC を押した場合の全リセットテストケースを追加する
  - タスク 3.1 と並行実行可能（異なるテストファイルを変更する）
  - タスク 2.2 完了後に実行すること
  - _Requirements: 6.2, 6.3_

- [x] 3.3 全テストの回帰確認を行う
  - エンジンテストと状態フローテストをそれぞれ実行し、すべてのテストが `passed` で終了することを確認する
  - 既存の `+` テストが引き算追加の影響を受けていないことを確認する
  - タスク 3.1 と 3.2 の両方が完了した後に実行すること
  - _Requirements: 6.4_
